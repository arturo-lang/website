name: Test

on:
    pull_request:
 
concurrency: 
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    Deployment:
        uses: ./.github/workflows/deploy.yml
        secrets: inherit
        with:
            channel: 'latest'
            test: true